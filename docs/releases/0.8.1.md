# `rvs` 0.8.1

`rvs` 0.8.1 keeps optional repository-resolve filters off the wire when they
are unset. Repository metadata and upstream commands no longer send empty
`customer_id` or registry-kind values that production interprets as invalid
identifiers.

The packaging regression test now follows the `v0.8` compatibility channel.
