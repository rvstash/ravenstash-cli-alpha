# `rvs` 0.8.5

`rvs` 0.8.5 retries transient transport failures for idempotent DevAPI GET
requests with bounded backoff. Mutating requests remain single-attempt, while
repository and private-mirror resolution no longer fail immediately on a single
control-plane read timeout.
